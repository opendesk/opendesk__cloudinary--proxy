{
  "private": true,
  "name": "opendesk__cloudinary--proxy",
  "version": "1.0.0",
  "description": "SAve som $ on Cloudinary by using a proxy",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "dev": "wrangler dev"
  },
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "dependencies": {
    "wrangler": "^4.13.2"
  }
}
